@model IEnumerable<LTH_PTPMQL_BKT.Models.Phongban>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.phong)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.phong)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Ten">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Ten">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Ten">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
